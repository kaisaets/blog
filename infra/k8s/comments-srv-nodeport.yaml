apiVersion: v1
kind: Service
metadata:
  name: comments-srv-nodeport
spec:
  type: NodePort
  selector: 
    app: comments
  ports:
    - name: comments
      protocol: TCP
      port: 5001
      targetPort: 5001